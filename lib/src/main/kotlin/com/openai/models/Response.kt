/**
 * Please note: This class is auto generated by OpenAPI Generator
 * (https://openapi-generator.tech). Do not edit this file manually.
 */
@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport",
)

package com.openai.models

import com.squareup.moshi.Json
import com.squareup.moshi.JsonClass

/**
 * @param metadata Set of 16 key-value pairs that can be attached to an object.
 *   This can be useful for storing additional information about the object in a
 *   structured format, and querying for objects via API or the dashboard. Keys
 *   are strings with a maximum length of 64 characters. Values are strings with
 *   a maximum length of 512 characters.
 * @param temperature What sampling temperature to use, between 0 and 2. Higher
 *   values like 0.8 will make the output more random, while lower values like
 *   0.2 will make it more focused and deterministic. We generally recommend
 *   altering this or `top_p` but not both.
 * @param topP An alternative to sampling with temperature, called nucleus
 *   sampling, where the model considers the results of the tokens with top_p
 *   probability mass. So 0.1 means only the tokens comprising the top 10%
 *   probability mass are considered. We generally recommend altering this or
 *   `temperature` but not both.
 * @param model
 * @param instructions Inserts a system (or developer) message as the first item
 *   in the model's context. When using along with `previous_response_id`, the
 *   instructions from a previous response will be not be carried over to the
 *   next response. This makes it simple to swap out system (or developer)
 *   messages in new responses.
 * @param tools An array of tools the model may call while generating a
 *   response. You can specify which tool to use by setting the `tool_choice`
 *   parameter. The two categories of tools you can provide the model are: -
 *   **Built-in tools**: Tools that are provided by OpenAI that extend the
 *   model's capabilities, like [web search](/docs/guides/tools-web-search) or
 *   [file search](/docs/guides/tools-file-search). Learn more about [built-in
 *   tools](/docs/guides/tools). - **Function calls (custom tools)**: Functions
 *   that are defined by you, enabling the model to call your own code. Learn
 *   more about [function calling](/docs/guides/function-calling).
 * @param toolChoice
 * @param id Unique identifier for this Response.
 * @param `object` The object type of this resource - always set to `response`.
 * @param createdAt Unix timestamp (in seconds) of when this Response was
 *   created.
 * @param error
 * @param incompleteDetails
 * @param output An array of content items generated by the model. - The length
 *   and order of items in the `output` array is dependent on the model's
 *   response. - Rather than accessing the first item in the `output` array and
 *   assuming it's an `assistant` message with the content generated by the
 *   model, you might consider using the `output_text` property where supported
 *   in SDKs.
 * @param parallelToolCalls Whether to allow the model to run tool calls in
 *   parallel.
 * @param user A unique identifier representing your end-user, which can help
 *   OpenAI to monitor and detect abuse.
 *   [Learn more](/docs/guides/safety-best-practices#end-user-ids).
 * @param previousResponseId The unique ID of the previous response to the
 *   model. Use this to create multi-turn conversations. Learn more about
 *   [conversation state](/docs/guides/conversation-state).
 * @param reasoning
 * @param maxOutputTokens An upper bound for the number of tokens that can be
 *   generated for a response, including visible output tokens and
 *   [reasoning tokens](/docs/guides/reasoning).
 * @param text
 * @param truncation The truncation strategy to use for the model response. -
 *   `auto`: If the context of this response and previous ones exceeds the
 *   model's context window size, the model will truncate the response to fit
 *   the context window by dropping input items in the middle of the
 *   conversation. - `disabled` (default): If a model response will exceed the
 *   context window size for a model, the request will fail with a 400 error.
 * @param status The status of the response generation. One of `completed`,
 *   `failed`, `in_progress`, or `incomplete`.
 * @param outputText SDK-only convenience property that contains the aggregated
 *   text output from all `output_text` items in the `output` array, if any are
 *   present. Supported in the Python and JavaScript SDKs.
 * @param usage
 */
data class Response(

    /* Set of 16 key-value pairs that can be attached to an object. This can be useful for storing additional information about the object in a structured format, and querying for objects via API or the dashboard.   Keys are strings with a maximum length of 64 characters. Values are strings with a maximum length of 512 characters.  */
    @Json(name = "metadata")
    val metadata: kotlin.collections.Map<kotlin.String, kotlin.String>?,

    /* What sampling temperature to use, between 0 and 2. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic. We generally recommend altering this or `top_p` but not both.  */
    @Json(name = "temperature")
    val temperature: java.math.BigDecimal? = java.math.BigDecimal("1"),

    /* An alternative to sampling with temperature, called nucleus sampling, where the model considers the results of the tokens with top_p probability mass. So 0.1 means only the tokens comprising the top 10% probability mass are considered.  We generally recommend altering this or `temperature` but not both.  */
    @Json(name = "top_p")
    val topP: java.math.BigDecimal? = java.math.BigDecimal("1"),
    @Json(name = "model") val model: ModelIdsResponses,

    /* Inserts a system (or developer) message as the first item in the model's context.  When using along with `previous_response_id`, the instructions from a previous response will be not be carried over to the next response. This makes it simple to swap out system (or developer) messages in new responses.  */
    @Json(name = "instructions") val instructions: kotlin.String?,

    /* An array of tools the model may call while generating a response. You  can specify which tool to use by setting the `tool_choice` parameter.  The two categories of tools you can provide the model are:  - **Built-in tools**: Tools that are provided by OpenAI that extend the   model's capabilities, like [web search](/docs/guides/tools-web-search)   or [file search](/docs/guides/tools-file-search). Learn more about   [built-in tools](/docs/guides/tools). - **Function calls (custom tools)**: Functions that are defined by you,   enabling the model to call your own code. Learn more about   [function calling](/docs/guides/function-calling).  */
    @Json(name = "tools") val tools: kotlin.collections.List<Tool>,
    @Json(name = "tool_choice") val toolChoice: ResponsePropertiesToolChoice,

    /* Unique identifier for this Response.  */
    @Json(name = "id") val id: kotlin.String,

    /* The object type of this resource - always set to `response`.  */
    @Json(name = "object") val `object`: Response.`Object`,

    /* Unix timestamp (in seconds) of when this Response was created.  */
    @Json(name = "created_at") val createdAt: java.math.BigDecimal,
    @Json(name = "error") val error: ResponseError?,
    @Json(name = "incomplete_details")
    val incompleteDetails: ResponseAllOfIncompleteDetails?,

    /* An array of content items generated by the model.  - The length and order of items in the `output` array is dependent   on the model's response. - Rather than accessing the first item in the `output` array and    assuming it's an `assistant` message with the content generated by   the model, you might consider using the `output_text` property where   supported in SDKs.  */
    @Json(name = "output") val output: kotlin.collections.List<OutputItem>,

    /* Whether to allow the model to run tool calls in parallel.  */
    @Json(name = "parallel_tool_calls")
    val parallelToolCalls: kotlin.Boolean = true,

    /* A unique identifier representing your end-user, which can help OpenAI to monitor and detect abuse. [Learn more](/docs/guides/safety-best-practices#end-user-ids).  */
    @Json(name = "user") val user: kotlin.String? = null,

    /* The unique ID of the previous response to the model. Use this to create multi-turn conversations. Learn more about  [conversation state](/docs/guides/conversation-state).  */
    @Json(name = "previous_response_id")
    val previousResponseId: kotlin.String? = null,
    @Json(name = "reasoning") val reasoning: Reasoning? = null,

    /* An upper bound for the number of tokens that can be generated for a response, including visible output tokens and [reasoning tokens](/docs/guides/reasoning).  */
    @Json(name = "max_output_tokens") val maxOutputTokens: kotlin.Int? = null,
    @Json(name = "text") val text: ResponsePropertiesText? = null,

    /* The truncation strategy to use for the model response. - `auto`: If the context of this response and previous ones exceeds   the model's context window size, the model will truncate the    response to fit the context window by dropping input items in the   middle of the conversation.  - `disabled` (default): If a model response will exceed the context window    size for a model, the request will fail with a 400 error.  */
    @Json(name = "truncation")
    val truncation: Response.Truncation? = Truncation.disabled,

    /* The status of the response generation. One of `completed`, `failed`,  `in_progress`, or `incomplete`.  */
    @Json(name = "status") val status: Response.Status? = null,

    /* SDK-only convenience property that contains the aggregated text output  from all `output_text` items in the `output` array, if any are present.  Supported in the Python and JavaScript SDKs.  */
    @Json(name = "output_text") val outputText: kotlin.String? = null,
    @Json(name = "usage") val usage: ResponseUsage? = null,
) {

    /**
     * The object type of this resource - always set to `response`.
     *
     * Values: response
     */
    @JsonClass(generateAdapter = false)
    enum class `Object`(val value: kotlin.String) {
        @Json(name = "response") response("response")
    }

    /**
     * The truncation strategy to use for the model response. - `auto`: If the
     * context of this response and previous ones exceeds the model's context
     * window size, the model will truncate the response to fit the context
     * window by dropping input items in the middle of the conversation. -
     * `disabled` (default): If a model response will exceed the context window
     * size for a model, the request will fail with a 400 error.
     *
     * Values: auto,disabled
     */
    @JsonClass(generateAdapter = false)
    enum class Truncation(val value: kotlin.String) {
        @Json(name = "auto") auto("auto"),
        @Json(name = "disabled") disabled("disabled"),
    }

    /**
     * The status of the response generation. One of `completed`, `failed`,
     * `in_progress`, or `incomplete`.
     *
     * Values: completed,failed,in_progress,incomplete
     */
    @JsonClass(generateAdapter = false)
    enum class Status(val value: kotlin.String) {
        @Json(name = "completed") completed("completed"),
        @Json(name = "failed") failed("failed"),
        @Json(name = "in_progress") in_progress("in_progress"),
        @Json(name = "incomplete") incomplete("incomplete"),
    }
}
